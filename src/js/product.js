const tabs = document.querySelectorAll(".product-tabs button"); const contentBlocks = document.querySelectorAll(".tab-content, .reviews-content"); tabs.forEach((tab, index) => { tab.addEventListener("click", () => { tabs.forEach(t => t.classList.remove("active")); tab.classList.add("active"); contentBlocks.forEach(content => content.style.display = "none"); if (tab.textContent.trim() === "Reviews") { document.querySelector(".reviews-content").style.display = "block"; } else { document.querySelectorAll(".tab-content")[0].style.display = "block"; } }); }); document.addEventListener("DOMContentLoaded", () => { const reviewForm = document.querySelector(".reviews-content .container-add-review"); const submitBtn = reviewForm.querySelector(".btn-submit"); const nameInput = reviewForm.querySelector('input[type="text"]'); const emailInput = reviewForm.querySelector('input[type="email"]'); const reviewTextarea = reviewForm.querySelector('textarea'); const messageEl = document.createElement("div"); messageEl.style.marginTop = "15px"; messageEl.style.fontWeight = "600"; reviewForm.appendChild(messageEl); submitBtn.addEventListener("click", (e) => { e.preventDefault(); const name = nameInput.value.trim(); const email = emailInput.value.trim(); const review = reviewTextarea.value.trim(); const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/; if (!name || !email || !review) { messageEl.textContent = "Please fill in all required fields."; messageEl.style.color = "red"; return; } if (!emailRegex.test(email)) { messageEl.textContent = "Please enter a valid email address."; messageEl.style.color = "red"; return; } messageEl.textContent = "Thank you! Your review has been submitted successfully."; messageEl.style.color = "green"; nameInput.value = ""; emailInput.value = ""; reviewTextarea.value = ""; }); });